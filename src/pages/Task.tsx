// @flow
import * as React from 'react';
import SideMenu from '../components/SideMenu';

type Props = {};

const Task = (props: Props) => {
  return (
    <div>
      <SideMenu page={'Task'} />
    </div>
  );
};

export default Task;
